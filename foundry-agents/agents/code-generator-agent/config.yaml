agent:
  name: code-generator-agent
  model: gpt-4
  temperature: 0.4
  max_tokens: 8000
  timeout: 120s
  mcp_tools:
    - company-design-system
  description: >
    Generates complete React and Vue project code using the selected
    design system components. Produces all source files, configs, and build setup.
